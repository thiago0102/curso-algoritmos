algoritmo "3 dias pra terminar"
var
  imp, par: real
inicio
 escreval ("qual valor do emprestimo")
  leia (imp)
  par <-(imp * 20)/100
  escreval("quantas parcelas")
  leia (par)
   par <-  1200/10

  escreval ("vou pagar 10 parcelas de" , par )
fimalgoritmo
